{
  "name": "progress-indicator",
  "version": "1.0.0",
  "homepage": "https://nenoproo.github.io/progress-indicator",
  "dependencies": {
    "@tailwindcss/cli": "^4.0.6",
    "tailwindcss": "^4.0.6"
  },
  "scripts": {
    "tailwind": "npx @tailwindcss/cli -i ./src/input.css -o ./src/output.css --watch",
    "build": "tailwindcss -i src/input.css -o build/output.css --minify",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
